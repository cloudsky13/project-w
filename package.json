{
  "name": "my-app",
  "version": "0.1.0",
  "private": false,
  "publishConfig": {
    "registry": "http://localhost:8081/repository/Node-artifacts/"
  },
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "sonarqube-scanner": "x.x.x"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sonar": "node sonar-project.js"
  }
}
